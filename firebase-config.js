// Paste your Firebase web app config here (Firebase console > Project settings > Your apps > Web app).
// Until you do, the site runs in DEMO MODE and stores everything in this browser only.
export const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};

// The one account allowed to use the admin panel (must match firestore.rules).
export const ADMIN_EMAIL = "emily@parentdata.org";
export const CONTACT_EMAIL = "emily@parentdata.org";
